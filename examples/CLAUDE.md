# examples/

Two working demos. Start with `legacy-refactor/` — fastest path to seeing skills in action.

| Demo | What it shows | Time to run |
|---|---|---|
| `legacy-refactor/` | Before/after refactor with /refactoring + /unit-testing | 5 min |
| `data-pipeline/` | Full ML training pipeline with Hydra + Lightning | 10 min |

Open either folder in Claude Code. The `CLAUDE.md` in each folder tells Claude how to work with that codebase.
